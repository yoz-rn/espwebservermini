THIS `data` FOLDER WILL BE UPLOADED TO THE ESP FLASH MEMORY (LittleFS PARTITION)
ENSURE A MAXIMUM FOLDER SIZE OF < 1 MB

use `.avif` OR `.webp` for assets and textfile to `.gz`
File size is the absoulte priority. Please, not more than 25 KB for each asset

Use the [script](/compress_asset.sh) `compress_asset.sh` to automate file compression from `/assets` to `/data/assets`. It is a major help especially if `.txt` formatted (ASCII art).

PROCEED W/ CAUTION