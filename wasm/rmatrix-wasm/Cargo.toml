[package]
name = "rmatrix-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"

# [package.metadata.wasm-pack.profile.release]
# wasm-opt = ["-Oz", "--strip-debug", "--strip-producers"]